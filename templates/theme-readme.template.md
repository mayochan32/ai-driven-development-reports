# <number> テーマ名

このテーマが扱う問い、対象読者、成果物を簡潔に説明します。

## 主な入口

| パス | 役割 |
|---|---|
| `<number>-<slug>.md` | 正本Markdown |
| `<number>-<slug>.html` | 人間向けHTMLビュー |
| `manifest.json` | テーマのメタデータ |
| `assets/` | 画像、データ、評価結果など。必要な場合のみ |
| `tools/` | このテーマ専用の生成・検証ツール。必要な場合のみ |
| `skills/` | このテーマから生まれた再利用スキル。必要な場合のみ |
| `.claude/` | Claude Code連携。必要な場合のみ |
| `.github/` | GitHub Copilot連携。必要な場合のみ |

## 利用方法

このテーマを読む手順、再生成方法、検証コマンドを記載します。

## 配布単位

このフォルダだけをダウンロードすれば、このテーマに関連する公開成果物と再現に必要なファイルが揃います。
